# SkillSense (Hackathon MVP)

Purpose: extract skills from resumes / LinkedIn / GitHub text, compare to role profiles, and recommend learning steps.

Run:
1. Create venv and activate
2. pip install -r requirements.txt
3. python -m spacy download en_core_web_sm
4. streamlit run app.py
